<Project>
  <!-- IsCakeTestProject / IsPackable are set in the csproj after Directory.Build.props. -->
  <PropertyGroup Condition="'$(IsCakeTestProject)' == 'true' Or '$(IsPackable)' == 'false'">
    <EnforceCodeStyleInBuild>false</EnforceCodeStyleInBuild>
  </PropertyGroup>
</Project>
